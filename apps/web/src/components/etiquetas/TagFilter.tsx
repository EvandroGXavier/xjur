
export function TagFilter({
  value,
  onChange,
  comTags,
  semTags,
  onComTagsChange,
  onSemTagsChange,
}: any) {
  return (
    <div className="flex items-center gap-2 p-2 border rounded-md">
       <span className="text-sm text-gray-500">Filtro de Etiquetas (Stub)</span>
       {/* Minimal implementation to avoid crashes */}
    </div>
  );
}
