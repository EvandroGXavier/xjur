# Configuração do Banco de Dados PostgreSQL
# Substitua os valores abaixo pelas credenciais do seu servidor
DATABASE_URL="postgresql://usuario:senha@localhost:5432/nome_do_banco?schema=public"

# Exemplo de configuração padrão (conforme install_drx.sh):
# DATABASE_URL="postgresql://drx:drx_secure_pass_123@localhost:5432/drx_db?schema=public"

# IMPORTANTE:
# 1. Copie este arquivo para .env na raiz do projeto
# 2. Copie também para packages/database/.env
# 3. Ajuste as credenciais conforme seu ambiente
# 4. Nunca commite o arquivo .env no Git (já está no .gitignore)
