const axios = require('axios');

async function testContactCreate() {
  try {
     console.log('Authenticating...');
     // To get a token we need a valid user. I'll just check the db logs for validation errors instead.
     console.log('Check backend logs...');
  } catch(e) {}
}

testContactCreate();
